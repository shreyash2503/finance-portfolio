// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
model User {
  id            String       @id @default(uuid()) // Unique ID for each user
  email         String       @unique                      // User's email, unique for login
  passwordHash  String       
  portfolios    Portfolio[]  
  createdAt     DateTime     @default(now())              
  updatedAt     DateTime     @updatedAt                   
  jwtToken      String?     
}

// Defines the Portfolio model
model Portfolio {
  id            String       @id @default(uuid()) 
  name          String       
  description   String?      
  userId        String       
  user          User         @relation(fields: [userId], references: [id]) 
  items         PortfolioItem[] 
  performances  PortfolioPerformance[] 
  createdAt     DateTime     @default(now())              
  updatedAt     DateTime     @updatedAt                  
}

// Defines the PortfolioItem model
model PortfolioItem {
  id            String       @id @default(uuid()) // Unique ID for each item
  name          String      
  symbol        String      
  quantity      Float        
  price         Float        
  portfolioId   String       
  portfolio     Portfolio    @relation(fields: [portfolioId], references: [id]) // Relationship to Portfolio
  // transactions  Transaction[] // One-to-many relationship with Transaction
  createdAt     DateTime     @default(now())              
  updatedAt     DateTime     @updatedAt                  
}

// This model will store information about transactions (buy/sell operations)
// model Transaction {
//   id              Int          @id @default(autoincrement()) // Unique ID for each transaction
//   portfolioItemId Int        // Foreign key to PortfolioItem model
//   portfolioItem   PortfolioItem @relation(fields: [portfolioItemId], references: [id]) // Relationship to PortfolioItem
//   action          String       // Action type: 'BUY' or 'SELL'
//   quantity        Float        // Quantity of assets in the transaction
//   price           Float        // Price at the time of the transaction
//   date            DateTime     @default(now())              // Transaction date
//   createdAt       DateTime     @default(now())              // Creation timestamp
//   updatedAt       DateTime     @updatedAt                   // Last updated timestamp
// }

// Optional: User can also track the performance of their portfolios
model PortfolioPerformance {
  id            String          @id @default(uuid()) 
  portfolioId   String       
  portfolio     Portfolio    @relation(fields: [portfolioId], references: [id]) 
  date          DateTime    
  totalValue    Float       
  createdAt     DateTime     @default(now())            
  updatedAt     DateTime     @updatedAt                   
}

